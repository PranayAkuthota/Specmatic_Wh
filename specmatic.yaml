version: 3

systemUnderTest:
  service:
    description: WorkHive APIs
    definitions:
      - definition:
          source:
            $ref: "#/components/sources/localFilesystem"
          specs:
            - auth.yaml
            - tasks.yaml
            - tenants.yaml
            - workspaces.yaml
    runOptions:
      openapi:
        baseUrl: http://localhost:8000

#dependencies:
#  services:
#    - service:
#        $ref: "#/components/services/workhive"

specmatic:
  settings:
    test:
      schemaResiliencyTests: all

components:
  sources:
    localFilesystem:
      filesystem:
        directory: ./api-specs
